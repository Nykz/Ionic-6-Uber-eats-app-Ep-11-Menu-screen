import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.uberEatsApp',
  appName: 'Uber Eats',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
